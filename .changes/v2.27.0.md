## v2.27.0 - 2026-08-07
### Added
* Added `--check-commits` flag (default `true`) to `cloud register-component-version`. Set it to `false` to register the version hash without looking up and pushing the commits since the last version
### Dependency
* chore(deps): bump the github-actions group with 3 updates
