kind: Changed
body: '`update --cloud` now checks all components concurrently instead of one at a time. Each component is a single independent lookup, so the cloud check uses the same worker pool as the git check'
time: 2026-08-10T14:00:03.000000000Z
