kind: Fixed
body: 'Warn when `--branch` is combined with `--auto` on `cloud register-component-version`. In auto mode the branch is read from the checked out branch, so the flag was silently ignored'
time: 2026-08-06T12:00:04.000000000Z
